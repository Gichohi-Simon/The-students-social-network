import {makeStyles} from '@material-ui/core/styles'

export default makeStyles((theme) => ({
    comment:{
        display:'flex',
        justifyContent:'center',
        width:'80%'
    },
    commentSection:{
        borderRadius:'25px',
    }
}));