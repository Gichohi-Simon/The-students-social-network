import {makeStyles} from '@material-ui/core/styles'

export default makeStyles((theme) => ({
    comment:{
        margin:'20px',
        display:'flex',
        justifyContent:'center',
        width:'80%',
        borderRadius:'25px'

    },
    commentSection:{
        borderRadius:'2px',
    }
}));